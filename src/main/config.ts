import path from 'node:path';
export const PRELOAD_FILE_PATH = path.join(path.dirname(__dirname), 'preload', 'index.js');
export const INDEX_FILE_PATH = path.join(path.dirname(__dirname), 'renderer', 'index.html');
